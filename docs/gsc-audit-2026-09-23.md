# sotyn.ai — Search Console setup + technical SEO audit

**Date:** 2026-09-23 · **Property:** https://www.sotyn.ai (apex 308 → www)
**Method:** live crawl of all 41 sitemap URLs + source review. No Google Search
Console API access in this session, so nothing below is GSC *performance* data —
it is the crawl-side equivalent of what GSC's Pages/Enhancements reports check.

---

## 1. Google Search Console — status and setup

**Not yet verified.** There was no `google-site-verification` tag, no
`google*.html` file in `public/`, and no DNS TXT wired into the repo.

A verification slot is now in place (see CHANGE LOG below). Two routes:

### Route A — Domain property (recommended)
Covers `sotyn.ai`, `www.sotyn.ai`, `http://` and every subdomain in one property,
and survives redeploys because it lives in DNS, not in the build.

1. Search Console → Add property → **Domain** → enter `sotyn.ai`
2. Copy the TXT record Google shows (`google-site-verification=...`)
3. At the DNS host for sotyn.ai, add: type `TXT`, name `@`, value = that string
4. Wait for propagation, then click Verify

### Route B — HTML tag (fallback / second property)
1. Search Console → Add property → **URL prefix** → `https://www.sotyn.ai`
2. Choose "HTML tag", copy **only the token** from `content="..."`
3. Paste it into `src/config/site.ts` → `googleSiteVerification`
4. Deploy, then click Verify

### After verification
- Submit `https://www.sotyn.ai/sitemap-index.xml` under Sitemaps
- Set the international target to India (or leave auto — `en-IN` is already declared)
- Request indexing for `/`, `/pricing`, `/epc-erp-software`, `/demo`
- Also verify the apex as its own property if you use Route B, so the 308 is visible

---

## 2. Technical crawl — results

**41/41 sitemap URLs return HTTP 200.** No redirects, no soft-404s, no broken
sitemap entries.

| Check | Result |
|---|---|
| HTTP status, all 41 pages | ✅ 200 |
| Canonical present & self-referencing | ✅ 41/41, all match the sitemap URL |
| Duplicate titles | ✅ none |
| Duplicate meta descriptions | ✅ none |
| `<h1>` count per page | ✅ exactly 1 on every page |
| Robots meta | ✅ `index, follow, max-image-preview:large` |
| apex → www | ✅ 308 permanent to `https://www.sotyn.ai/` |
| robots.txt | ✅ allows all + GPTBot/ClaudeBot/PerplexityBot/Google-Extended; sitemap declared |
| Sitemap | ✅ `sitemap-index.xml` → `sitemap-0.xml`, 41 URLs, no trailing slashes |
| noindex pages excluded from sitemap | ✅ `/thank-you`, `/social-kit` are `noindex, nofollow` and absent |
| 404 handling | ✅ real HTTP 404 |
| `<html lang>` | ✅ `en-IN` |
| Image `alt` (homepage) | ✅ 14/14 have alt |
| Structured data | ✅ Organization, SoftwareApplication, AggregateOffer, FAQPage (6 Q&A) |
| Open Graph / Twitter cards | ✅ complete, absolute image URLs |
| Security headers | ✅ HSTS preload, X-Frame-Options, nosniff, Referrer-Policy |
| Homepage TTFB / transfer | ✅ 0.25 s, 25 KB compressed |

### Issues found

**1. Titles over 60 characters — 28 of 41 pages. → FIXED.**
Longest were `/tools` (99 chars) and `/resources` (97). All 41 indexable pages are
now ≤60 characters, with no duplicates introduced.

> Correction to the first pass of this report: it said *31* titles and measured
> them in **bytes**. `—` and `₹` are 3 bytes each in UTF-8, so those figures were
> inflated. The character counts above are the correct ones.

**2. Meta descriptions over 160 characters — 32 of 41 pages. → FIXED.**
Longest were `/pricing` (273 chars), `/platform` (267) and `/about` (257). All 41
are now ≤160, most at 130–155.

**3. No analytics or GSC-linked measurement.** There is no GA4 / gtag on the site
and `metaPixelId` is empty, so once GSC is verified you will have search-side data
with nothing to join it to on the site side.

**4. Render-blocking Google Fonts stylesheet** in `<head>` (Anton + Inter
variable). `preconnect` is already there; `display=swap` is set. Self-hosting the
two families would remove the third-party round trip — optional, low priority
given the 0.25 s TTFB.

**5. No `hreflang`.** Correct today (single `en-IN` site). Only relevant if you
add regional or Hindi variants.

### Not blocking, worth noting
- `/demo`, `/webinar`, `/scorecard` are conversion pages that are indexable — fine,
  but they will pull branded queries away from `/`. Watch in GSC once data lands.
- `sales@sotyn.ai` is marked ⚠️ REPLACE in config; the schema uses
  `emailFallback` (`sales@securedengineers.com`), so structured data is consistent.

---

## 3. Semrush / organic visibility

Requested, **not delivered** — the Semrush account has an active subscription but
no API units left, so `domain_overview` returned `no_api_units`. Top up at
https://www.semrush.com/mcp-access and this section can be filled in.

---

## 4. Priority actions

| # | Action | Effort | Impact |
|---|---|---|---|
| 1 | Verify the **domain property** via DNS TXT | 10 min | Unblocks everything |
| 2 | Submit `sitemap-index.xml` | 1 min | Indexation |
| 3 | ~~Trim the 28 titles over 60 chars~~ | done | CTR |
| 4 | ~~Trim the 32 descriptions over 160 chars~~ | done | CTR |
| 5 | Add GA4 and link it to the GSC property | 20 min | Query → conversion attribution |
| 6 | Re-run this crawl after 4 weeks of GSC data | — | Compare crawl vs. real coverage |

---

## CHANGE LOG — what was edited in this repo

- `src/config/site.ts` — added `googleSiteVerification` (empty by default,
  marked ⚠️ REPLACE) under a new "Search Console" block.
- `src/layouts/BaseLayout.astro` — renders
  `<meta name="google-site-verification" content="...">` sitewide **only** when
  that value is non-empty.

Verified: `npm run build` succeeds (44 pages); tag appears on every page when the
token is set and is absent when empty; `npm test` → 138/138 pass.

### Second pass — title & description rewrite

60 edits across 24 files (17 page files, `src/config/calculators.ts`,
`src/pages/compare/[slug].astro`, `src/pages/solutions/[slug].astro`,
`src/pages/webinar.astro` template strings). Every replacement was matched against
the exact existing string and asserted unique before any file was written.

**Claims:** no new claim was introduced anywhere. Contested claims that survive
the trim untouched — `AI BOQ quoting` (`/solutions/mep-contractors`,
`/compare/sotyn-vs-rdash`), `AI quotations` (`/features`), `geofenced attendance`
(`/solutions/civil-contractors`) — are unchanged, so `claims-registry.md`
DECISIONS 5–9 are unaffected.

**One claim left the index:** the homepage meta description previously read
*"…in one login, in 11 languages. Live in 30 days. If it's not in SOTYN, it
didn't happen."* Trimming to 160 chars dropped **"in 11 languages"**, which
`claims-registry.md` §3 flags as **DECISION 4** (present tense on the homepage,
"rolling out" on `/demo` and `/reduce-project-delays`, `["en","hi"]` in schema).
The registry specifically noted this claim was what a buyer sees in search
results. It is now not. **If DECISION 4 resolves as "11 languages are live",
put it back** — there is room at 136 characters.

Verified after the rewrite: `npm run build` → 44 pages; `npm test` → 138/138 pass;
0 of 41 indexable pages over the title or description limit; still zero duplicate
titles or descriptions.

**Not yet live.** Per `CHANGE_LEDGER.md`, production still serves the pre-fix
code from `main`; this work sits on `fix/audit-2026-09-23` behind PR #8.

---

# Third pass — Google tools setup (2026-09-23, via Chrome)

Account driving all of this: **director@securedengineers.com** (Workspace, managed
by kalsi.co.in).

## The DNS TXT record for sotyn.ai

Add at GoDaddy → sotyn.ai → DNS → Add record:

| Field | Value |
|---|---|
| Type | `TXT` |
| Name / Host | `@` |
| Value | `google-site-verification=TVdC_GjFILrpoaFQ7ZsX540D9j1_Q2Iq9sRpCSGWD74` |
| TTL | default (1 hour) |

Then Search Console → the `sotyn.ai` domain property → **Verify**.

⚠️ The value above is the FULL record. Search Console's own input box visually
truncates it at `…Q2Iq9sRpCS` — the real token ends `…GWD74`. Read it from here,
not from a screenshot of that field.

⚠️ Re-check the token before you paste. The browser session was reset mid-setup,
so if the pending `sotyn.ai` property did not persist, Search Console will issue a
**new** token when you re-create it, and this one will not verify.

## Do NOT use the automated GoDaddy flow (recommended)

Search Console auto-detects GoDaddy and offers "START VERIFICATION", which runs
GoDaddy **Domain Connect**. That template's scope is
`domain-verification+gmail-setup` — it can write **MX records**, not only the
verification TXT. On a domain whose email you already run, that is an unnecessary
risk. The manual TXT record above changes nothing except verification.

A Domain Connect tab may still be open in Chrome from this session. It is inert
(GoDaddy was never signed into) — just close it.

## State found

| Tool | State before | Action |
|---|---|---|
| Search Console | **Empty.** One pending, unverified `securedengineers.com` domain property. Nothing for sotyn.ai | Created `sotyn.ai` domain property, pending DNS |
| Google Analytics | One GA4 property, `www.securedengineers.com` (live, receiving traffic). **Nothing for sotyn.ai** | Blocked — see below |
| Google Ads | An Ads account exists (the GA account is named "Google Ads Account"), so Keyword Planner needs no new account | Not reached |
| Google Tag Manager | Not checked | Blocked |
| Business Profile | Not checked | Blocked |

## Blocked, and why

Google demanded re-authentication ("Verify it's you") partway through, and GoDaddy
was never signed in. **Entering passwords is not something I do**, so these stop
with you:

1. Re-authenticate the Google session in Chrome.
2. Add the TXT record at GoDaddy (or tell me to drive the form once you're signed in).

Still outstanding after that: GA4 property + web stream for sotyn.ai, GTM
container, sitemap submission, Keyword Planner, Business Profile.

## Code prepared while blocked

`SITE.analytics` now has two **mutually exclusive** slots, because
`lead-and-analytics-spec.md` §2 says *"One stack only. Do not add a second tag
manager or a duplicate GA4 tag."*

- `ga4MeasurementId` — `G-XXXXXXXXXX`, loads gtag.js directly. Simplest.
- `gtmContainerId` — `GTM-XXXXXXX`, loads GTM; configure GA4 **inside** GTM.

Setting both throws at build time, so the spec cannot be violated by accident.
Both empty (the state today) means no analytics loads at all. `anonymize_ip` is
on and no PII is passed, per spec §2. `tests/analytics.test.mjs` asserts the
mutual exclusion, both ID formats, that nothing fires unconfigured, and that the
GSC slot holds a bare token rather than a whole meta tag.

Verified: `npm run build` → 47 pages; `npm test` → 163/163 pass.

**Still unresolved and not mine to decide:** the spec notes every form should link
to a privacy policy and *"that page does not exist yet"*. Loading GA4 or GTM
before it does widens that gap.

---

# Fourth pass — what is actually LIVE in Google now (2026-09-23)

Account: **director@securedengineers.com**. All of this was done in the browser
and each line was read back from the product UI, not assumed.

## Done and verified

| # | Thing | Identifier | Evidence |
|---|---|---|---|
| 1 | **Search Console domain property — VERIFIED** | `sc-domain:sotyn.ai` | Overview dashboard loads; URL carried `after_verification_success` |
| 2 | DNS TXT written at GoDaddy | `google-site-verification=TVdC_GjFILrpoaFQ7ZsX540D9j1_Q2Iq9sRpCSGWD74` | Confirmed independently via public DNS (`dns.google`) |
| 3 | **No MX records were added** | — | `dns.google` MX query on sotyn.ai → no answer section. The `gmail-setup` scope did not touch mail |
| 4 | Sitemap submitted | `https://www.sotyn.ai/sitemap-index.xml` | "Sitemap submitted successfully" |
| 5 | **GA4 property** | `sotyn.ai` — property **555549868** | Admin header + property picker |
| 6 | GA4 web stream | `www.sotyn.ai` — stream **15831517000** | Data streams table |
| 7 | **GA4 Measurement ID** | **`G-85EHQ8CD1W`** | Read from Web stream details (see caveat) |
| 8 | **GTM container** | **`GTM-M7RVVGSG`** ("www.sotyn.ai", under the existing *Secured Engineers* account) | Container workspace header |
| 9 | GTM tag published | "GA4 — sotyn.ai" · Google Tag · `G-85EHQ8CD1W` · trigger *Initialization – All Pages* | **Version 2, published** 09/23/2026 |
| 10 | Site wired to GTM | `SITE.analytics.gtmContainerId = "GTM-M7RVVGSG"` | `npm run build` → 47 pages; `GTM-M7RVVGSG` present in `dist/index.html`; **`gtag/js` count = 0** |
| 11 | Tests | 163/163 pass | `npm test` |

**Measurement-ID caveat, stated because it nearly went wrong:** a GA4 splash
screen displayed a *different* ID (`G-LRMJ9E516E`) while the property header was
stale. The value above was re-read from the sotyn.ai stream's own **Web stream
details** panel, alongside stream ID 15831517000 and URL `https://www.sotyn.ai`.
That is the authoritative source and it is what went into GTM.

**One stack, as the spec demands.** `docs/lead-and-analytics-spec.md` §2 says
*"One stack only. Do not add a second tag manager or a duplicate GA4 tag."* GTM is
the only tag on the page; GA4 runs **inside** GTM. `ga4MeasurementId` is
deliberately left empty and the build throws if both are ever set.

**Search Console verification slot is now redundant.** `googleSiteVerification`
stays empty on purpose — ownership is held by the DNS TXT record, which is
stronger (covers apex + www + http, survives redeploys). Leave it empty.

## Not finished

| Thing | State |
|---|---|
| **GA4 ↔ Search Console link** | Reached the final step (choosing the `www.sotyn.ai` data stream) and the Chrome extension dropped. **Not linked.** Redo: GA4 → Admin → Product links → Search Console links → Link → `sotyn.ai` (Domain) → stream `www.sotyn.ai` → Submit |
| **Keyword Planner** | Not reached. A Google Ads account exists, so no new account is needed |
| **Google Business Profile** | Not reached. Verification is by post/phone and takes days |
| Sitemap status | Showed **"Couldn't fetch"** immediately after submission. The sitemap itself returns HTTP 200, so this is most likely the not-yet-processed state — **re-check in a day** rather than treating it as done |

## THE BLOCKER — nothing collects data until you deploy

Production still serves the pre-fix code from `main`. The GTM snippet lives on
`fix/audit-2026-09-23` behind PR #8. Until that is merged and deployed:

- GTM loads on **no** page, so GA4 receives **nothing** (the stream correctly
  reads "No data received").
- The title/description rewrites from the second pass are not live either.

Search Console is unaffected — it is verified by DNS and is already collecting.

## Indexation — new finding, no login required

`site:sotyn.ai` on 2026-09-23 surfaced **~13 pages against 41 in the sitemap**.

Indexed: `/`, `/demo`, `/tools`, `/features`, `/webinar`, `/pricing`, `/platform`,
`/about`, `/scorecard`, `/solutions/industrial-epc`, `/tools/erp-roi`,
`/tools/delay-cost`, `/compare/sotyn-vs-procore`.

Absent from those results — and these are the commercial pages:
`/epc-erp-software`, `/ra-billing-software`, `/construction-procurement-software`,
`/subcontractor-billing-software`, `/material-reconciliation`,
`/reduce-project-delays`, `/construction-erp-implementation`, five of six
`/compare/*`, three of four `/solutions/*`, all `/resources/*`, most `/tools/*`.

Caveats: `site:` is approximate and does not enumerate an index. Treat this as a
signal to check against **Search Console → Pages** once it has data, which is now
possible for the first time.

Two supporting observations:
- **Apex and www are both in the index** (`https://sotyn.ai/tools` alongside
  `https://www.sotyn.ai/...`) despite the 308 and www canonicals. The domain
  property covers both, which is the right shape for diagnosing it.
- **Indexed titles are stale** — Google still shows "EPC ERP Software for
  Contractors in India" for `/` and "AI Construction Operating System for India"
  for `/platform`, neither of which matches current source. Consistent with
  production lagging the repo.

## Unrelated finding worth acting on

**sotyn.ai has no MX records at all.** `sales@sotyn.ai` — used as `email` and
`emailDisplay` in `src/config/site.ts` and already flagged ⚠️ REPLACE — would
**bounce** today. Structured data uses `emailFallback`
(`sales@securedengineers.com`), so the schema is safe, but any human who reads
`sales@sotyn.ai` off the site cannot reach you.

## Google Trends — deliberately not repeated

`seo-audit/google-trends-research.csv` (from the parallel workstream) already
queried India 5-year and 12-month for 44 keyword-timeframe combinations. Its
finding: **every term sits at or below Google's reporting threshold for India** —
the densest series had 6.1% of weeks above zero, and the related-queries widget
came back empty for 44 of 44. Trends cannot size this market. That makes
**Keyword Planner the only remaining source of real volume data**, which is why
it matters more than the rest of the leftovers.

---

# Fifth pass — 2026-09-24

## Completed

| Thing | Result |
|---|---|
| **GA4 ↔ Search Console link** | **Linked.** `sotyn.ai` (Domain) ↔ stream `www.sotyn.ai` / `15831517000`, by director@securedengineers.com, Sep 24 2026 |
| **Sitemap** | **Success** — read Sep 23, **44 pages discovered**. The "Couldn't fetch" was the pre-processing state, as expected |
| **Production redeployed** | Homepage title is now `Construction ERP Software for Indian Contractors \| sotyn.ai` — the second-pass rewrites are **live** |
| **GTM still NOT live** | `curl` of production: 0 occurrences of `GTM-M7RVVGSG`, 0 of `gtag/js`. The `gtmContainerId` line is committed nowhere yet |

## Keyword Planner — FIRST AUTHORISED VOLUME DATA FOR THIS PROJECT

Google Ads account **474-400-7750 (secured engineers pvt ltd)**. Plan saved as
"Plan from Sep 24, 2026" (planId 1439012562). Geo **India**, Google search only,
Sep 2025 – Aug 2026.

**Read this first:** the account has no active campaign, so Google returns
**banded** volumes, not exact numbers ("To get more detailed statistics, run a
campaign"). Bands and competition are real data; treat them as ranges. This
supersedes `keyword-map.md`'s "no authorised volume data exists".

| Keyword | Avg monthly searches (India) | Competition | Top-of-page bid |
|---|---|---|---|
| **construction erp software** | **1K – 10K** | **Low** | ₹58.38 – ₹287.28 |
| construction erp software india | 100 – 1K | Low | ₹77.69 – ₹352.80 |
| construction billing software | 100 – 1K | Medium | ₹38.90 – ₹163.29 |
| construction project management software india | 100 – 1K | Medium | ₹63.37 – ₹251.55 |
| construction procurement software | 10 – 100 | Low | ₹91.74 – ₹437.50 |
| epc erp software | 10 – 100 | **High** | ₹80.87 – ₹325.71 |
| erp for construction company | 10 – 100 | Medium | ₹63.65 – ₹319.59 |
| construction software india | 10 – 100 | Medium | ₹57.73 – ₹229.39 |
| construction labour management software | 10 – 100 | High | ₹84.66 – ₹252.04 |
| solar epc software | 10 – 100 | Medium | ₹30.91 – ₹144.62 |
| dpr software | 10 – 100 | Low | ₹43.85 – ₹97.46 |
| subcontractor billing software | 10 – 100 | Medium | — |

**No measurable volume in India** (below Google's reporting threshold):
`ra billing software`, `material reconciliation software`, `mep contractor
software`, `civil contractor erp`, `contractor erp software`, `boq management
software`, `measurement book software`, `tally integration for contractors`.

### What this changes

1. **`construction erp software` is the prize and nobody told us.** 1K–10K/month
   at **Low** competition — an order of magnitude above everything else here, and
   **there is no dedicated page for the unqualified term.** `keyword-map.md`
   routes it to `/` alongside "construction ERP software India" (100–1K). The
   bigger, easier term is the one without "india".
2. **`/epc-erp-software` is aimed at a weak term.** 10–100/month at **High**
   competition — the worst combination in the table. It is treated as a core
   commercial page. It should not be the flagship.
3. **`/ra-billing-software` targets a term with no measurable India volume.**
   `keyword-map.md` calls it "the strongest match on the site". By volume it is
   unmeasurable. The page may still earn long-tail traffic, but it cannot be a
   primary acquisition bet.
4. **Two unclaimed 100–1K terms:** `construction billing software` and
   `construction project management software india`. The latter maps to
   `/platform`, whose title now leads on exactly that phrase (second pass).
   `construction billing software` has no home.
5. The **Trends finding stands** — Trends reports nothing because these volumes
   sit near its threshold. Keyword Planner reads below that threshold, which is
   why it worked where Trends could not.

## Google Business Profile — NOT created, deliberately

A profile for **Secured Engineers already exists and this account manages it**
(Google showed the "Your business on Google … only managers of this profile can
see this" panel). It was not opened or modified.

A **separate sotyn.ai profile was not created.** Google's own guidelines
(support.google.com/business/answer/3038177), checked 2026-09-24:

- *"If your business either has a physical location that customers can visit, or
  travels to customers where they are, you can create a Business Profile."*
  sotyn.ai is SaaS with no in-person customer contact — it fails this test.
- *"Do not create more than one page for each location of your business, either
  in a single account or multiple accounts."* sotyn.ai shares the Ludhiana
  address with Secured Engineers.
- The department exception needs a *publicly-facing department operating as a
  distinct entity* with a distinct name **and** category. A software product of
  the same legal entity does not qualify.

Creating it risks suspension, and suspension can reach other listings the account
manages — including the live Secured Engineers profile. The upside is small: B2B
ERP buyers do not use Maps. The `Organization` + `SoftwareApplication` JSON-LD
already on every page is the correct brand-presence mechanism, and it is live.

**Decision left to the client.** If a sotyn.ai profile is ever wanted, the safe
route is a genuinely distinct presence — its own registered entity and its own
office/suite with its own signage and staff — not a second pin at 2480/1 BK Tower.
