# TOOL_ACCESS — what was attempted, what came back, what is required

Every row is an attempt I made myself, with the date. Nothing here is inferred, and no blocked metric was filled in with an estimate. Re-checked 2026-09-23.

| Tool | Property / account used | Query or settings | Date | Result | Evidence | Exact requirement to unblock |
|---|---|---|---|---|---|---|
| **Google Search Console** | none reachable for sotyn.ai | — | 2026-09-23 | **BLOCKED** — no authorised connector in this session; the previously connected route exposed **Secured Engineers** properties only, which must not be mixed with Sotyn reporting | session connector list | Verify/share the **`https://www.sotyn.ai`** property (and the `sotyn.ai` Domain property), grant **Full** access, or export Performance: 16 months, query + page + country + device, CSV |
| **GA4** | none | — | 2026-09-23 | **BLOCKED**, and note: **no GA4 or GTM tag is present in the live HTML**, so a property may not exist at all | live page source contains no `gtag`/`gtm.js` | Confirm whether a property exists; if yes **Viewer** + measurement ID; if no, decide before any tag is added (one stack only) |
| **GTM / Tag Assistant / DebugView** | none | — | 2026-09-23 | **BLOCKED** — nothing to audit for duplicate `page_view`/`generate_lead` because no container is installed | as above | Container ID + publish rights, or a decision to tag directly in the layout |
| **Google Ads / Keyword Planner** | none | India, INR intended | 2026-09-23 | **BLOCKED** — no authorised Ads account | — | **Read** access to an existing Ads account. Do **not** start a campaign to unlock ranges. Advertiser competition ≠ organic difficulty |
| **Google Trends** | — | India, 12 m and 5 y intended | 2026-09-23 | **BLOCKED** — no usable export obtained | — | A session that can export the CSV with the exact term/topic definitions recorded. Values are relative interest, never volume |
| **PageSpeed Insights (keyless)** | public endpoint | `/`, `/pricing`, `/tools/ra-bill`, `/solutions/mep-contractors`, mobile + desktop | 2026-09-23 (retried today) | **BLOCKED — HTTP 429**: *"Quota exceeded for quota metric 'Queries' and limit 'Queries per day' … quota_limit_value: 0"*. A control run against `example.com` also 429s, so the block is global to the keyless endpoint | quoted API error | A free **PSI API key** (`&key=…`), or run from pagespeed.web.dev / Chrome DevTools |
| **CrUX API (field data)** | public endpoint | origin `https://www.sotyn.ai` | 2026-09-23 (retried today) | **BLOCKED — HTTP 403**: *"Method doesn't allow unregistered callers"* | quoted API error | A CrUX/PSI API key. Until then **no p75 LCP / INP / CLS figure exists** — and none has been invented |
| **schema.org validator** | public | JSON-LD extracted from the branch build | 2026-09-23 | **SUCCESS — 0 errors, 0 warnings** on `/`, `/pricing`, `/scorecard`, `/webinar`, `/compare/sotyn-vs-onsite` | validator API response | — (note: syntax validity, **not** Google rich-result eligibility) |
| **Rich Results Test** | — | — | 2026-09-23 | **BLOCKED** — no scriptable endpoint; also moot for FAQ, which Google no longer surfaces as rich results | — | Manual run in Search Console when access exists |
| **Ahrefs** | plugin | India keyword metrics | earlier + today | **BLOCKED** — `Insufficient plan`; the plugin also requires OAuth that cannot run in this non-interactive session | plugin auth state | A plan covering Site Explorer + Keywords Explorer, and an interactive `/mcp` authorisation |
| **Semrush / SEOcrawl** | plugin | — | 2026-09-23 | **BLOCKED** — Semrush connector unauthorised here; SEOcrawl previously returned `billing_context_missing` | session connector list | Interactive OAuth, or billing configured. Neither is required — the crawl in this work used plain HTTP |
| **Vercel dashboard** | — | production project + deployment metadata | 2026-09-23 | **BLOCKED** — no dashboard access. Provider confirmed from response headers instead (`server: Vercel`, `x-vercel-id: bom1::…`); **five** Vercel projects build this repo, and which one serves the domain is unknown from outside | commit statuses on PR #8 | Viewer access to the `sotyn-ai` project; confirm which project owns the domain and retire the other four |
| **Vercel preview HTML** | preview URL from PR #8 | `GET /`, `/pricing` | 2026-09-23 | **BLOCKED — 302 to `vercel.com/sso-api`** (deployment protection). Builds are green; the rendered preview could not be fetched anonymously | curl redirect chain | Disable protection for previews, or open the preview while signed in |
| **Lead webhook** `securederp.in/api/public/sotyn-lead` | production endpoint | `OPTIONS`, `GET` only | 2026-09-23 | **PARTIAL** — `OPTIONS` from `https://www.sotyn.ai` and `https://sotyn.ai` → **204** with CORS; any other origin → 200 **without** CORS (so previews and localhost are blocked by design); `GET` → clean JSON 404. **No POST was sent** — that would create a real CRM record | quoted headers | The success contract (status, `Content-Type`, body, lead-id field), whether it de-duplicates on `X-Request-Id`, and either a staging endpoint or written authority for one labelled test lead |
| **CRM / sales ledger** | — | — | 2026-09-23 | **BLOCKED** | — | Read access or a dated export, so accepted leads can be reconciled against real opportunities |
| **Google Business Profile** | — | — | 2026-09-23 | **Not attempted** — eligibility evidence is missing and a software site serving a city is not a branch there | — | Confirmation of genuinely staffed locations only |
| **Bing Webmaster Tools** | — | — | 2026-09-23 | **Not attempted** — no value until the canonical-host change ships | — | — |

## Rules applied

- A tool that could not be reached is recorded as **blocked**, never as "no traffic", "no demand" or "no issue".
- Missing GSC query rows would be a privacy threshold, not zero searches. No such data exists here either way.
- Paid calls were not retried indefinitely: PSI was attempted 14 times earlier and once today; CrUX twice. Both fail for account reasons, not transient load.
- No permission was bypassed, no access purchased, no campaign activated, and no number fabricated in place of a blocked measurement.
