# sotyn.ai — Search Console setup + technical SEO audit

**Date:** 2026-09-23 · **Property:** https://www.sotyn.ai (apex 308 → www)
**Method:** live crawl of all 41 sitemap URLs + source review. No Google Search
Console API access in this session, so nothing below is GSC *performance* data —
it is the crawl-side equivalent of what GSC's Pages/Enhancements reports check.

---

## 1. Google Search Console — status and setup

**Not yet verified.** There was no `google-site-verification` tag, no
`google*.html` file in `public/`, and no DNS TXT wired into the repo.

A verification slot is now in place (see CHANGE LOG below). Two routes:

### Route A — Domain property (recommended)
Covers `sotyn.ai`, `www.sotyn.ai`, `http://` and every subdomain in one property,
and survives redeploys because it lives in DNS, not in the build.

1. Search Console → Add property → **Domain** → enter `sotyn.ai`
2. Copy the TXT record Google shows (`google-site-verification=...`)
3. At the DNS host for sotyn.ai, add: type `TXT`, name `@`, value = that string
4. Wait for propagation, then click Verify

### Route B — HTML tag (fallback / second property)
1. Search Console → Add property → **URL prefix** → `https://www.sotyn.ai`
2. Choose "HTML tag", copy **only the token** from `content="..."`
3. Paste it into `src/config/site.ts` → `googleSiteVerification`
4. Deploy, then click Verify

### After verification
- Submit `https://www.sotyn.ai/sitemap-index.xml` under Sitemaps
- Set the international target to India (or leave auto — `en-IN` is already declared)
- Request indexing for `/`, `/pricing`, `/epc-erp-software`, `/demo`
- Also verify the apex as its own property if you use Route B, so the 308 is visible

---

## 2. Technical crawl — results

**41/41 sitemap URLs return HTTP 200.** No redirects, no soft-404s, no broken
sitemap entries.

| Check | Result |
|---|---|
| HTTP status, all 41 pages | ✅ 200 |
| Canonical present & self-referencing | ✅ 41/41, all match the sitemap URL |
| Duplicate titles | ✅ none |
| Duplicate meta descriptions | ✅ none |
| `<h1>` count per page | ✅ exactly 1 on every page |
| Robots meta | ✅ `index, follow, max-image-preview:large` |
| apex → www | ✅ 308 permanent to `https://www.sotyn.ai/` |
| robots.txt | ✅ allows all + GPTBot/ClaudeBot/PerplexityBot/Google-Extended; sitemap declared |
| Sitemap | ✅ `sitemap-index.xml` → `sitemap-0.xml`, 41 URLs, no trailing slashes |
| noindex pages excluded from sitemap | ✅ `/thank-you`, `/social-kit` are `noindex, nofollow` and absent |
| 404 handling | ✅ real HTTP 404 |
| `<html lang>` | ✅ `en-IN` |
| Image `alt` (homepage) | ✅ 14/14 have alt |
| Structured data | ✅ Organization, SoftwareApplication, AggregateOffer, FAQPage (6 Q&A) |
| Open Graph / Twitter cards | ✅ complete, absolute image URLs |
| Security headers | ✅ HSTS preload, X-Frame-Options, nosniff, Referrer-Policy |
| Homepage TTFB / transfer | ✅ 0.25 s, 25 KB compressed |

### Issues found

**1. Titles over 60 characters — 28 of 41 pages. → FIXED.**
Longest were `/tools` (99 chars) and `/resources` (97). All 41 indexable pages are
now ≤60 characters, with no duplicates introduced.

> Correction to the first pass of this report: it said *31* titles and measured
> them in **bytes**. `—` and `₹` are 3 bytes each in UTF-8, so those figures were
> inflated. The character counts above are the correct ones.

**2. Meta descriptions over 160 characters — 32 of 41 pages. → FIXED.**
Longest were `/pricing` (273 chars), `/platform` (267) and `/about` (257). All 41
are now ≤160, most at 130–155.

**3. No analytics or GSC-linked measurement.** There is no GA4 / gtag on the site
and `metaPixelId` is empty, so once GSC is verified you will have search-side data
with nothing to join it to on the site side.

**4. Render-blocking Google Fonts stylesheet** in `<head>` (Anton + Inter
variable). `preconnect` is already there; `display=swap` is set. Self-hosting the
two families would remove the third-party round trip — optional, low priority
given the 0.25 s TTFB.

**5. No `hreflang`.** Correct today (single `en-IN` site). Only relevant if you
add regional or Hindi variants.

### Not blocking, worth noting
- `/demo`, `/webinar`, `/scorecard` are conversion pages that are indexable — fine,
  but they will pull branded queries away from `/`. Watch in GSC once data lands.
- `sales@sotyn.ai` is marked ⚠️ REPLACE in config; the schema uses
  `emailFallback` (`sales@securedengineers.com`), so structured data is consistent.

---

## 3. Semrush / organic visibility

Requested, **not delivered** — the Semrush account has an active subscription but
no API units left, so `domain_overview` returned `no_api_units`. Top up at
https://www.semrush.com/mcp-access and this section can be filled in.

---

## 4. Priority actions

| # | Action | Effort | Impact |
|---|---|---|---|
| 1 | Verify the **domain property** via DNS TXT | 10 min | Unblocks everything |
| 2 | Submit `sitemap-index.xml` | 1 min | Indexation |
| 3 | ~~Trim the 28 titles over 60 chars~~ | done | CTR |
| 4 | ~~Trim the 32 descriptions over 160 chars~~ | done | CTR |
| 5 | Add GA4 and link it to the GSC property | 20 min | Query → conversion attribution |
| 6 | Re-run this crawl after 4 weeks of GSC data | — | Compare crawl vs. real coverage |

---

## CHANGE LOG — what was edited in this repo

- `src/config/site.ts` — added `googleSiteVerification` (empty by default,
  marked ⚠️ REPLACE) under a new "Search Console" block.
- `src/layouts/BaseLayout.astro` — renders
  `<meta name="google-site-verification" content="...">` sitewide **only** when
  that value is non-empty.

Verified: `npm run build` succeeds (44 pages); tag appears on every page when the
token is set and is absent when empty; `npm test` → 138/138 pass.

### Second pass — title & description rewrite

60 edits across 24 files (17 page files, `src/config/calculators.ts`,
`src/pages/compare/[slug].astro`, `src/pages/solutions/[slug].astro`,
`src/pages/webinar.astro` template strings). Every replacement was matched against
the exact existing string and asserted unique before any file was written.

**Claims:** no new claim was introduced anywhere. Contested claims that survive
the trim untouched — `AI BOQ quoting` (`/solutions/mep-contractors`,
`/compare/sotyn-vs-rdash`), `AI quotations` (`/features`), `geofenced attendance`
(`/solutions/civil-contractors`) — are unchanged, so `claims-registry.md`
DECISIONS 5–9 are unaffected.

**One claim left the index:** the homepage meta description previously read
*"…in one login, in 11 languages. Live in 30 days. If it's not in SOTYN, it
didn't happen."* Trimming to 160 chars dropped **"in 11 languages"**, which
`claims-registry.md` §3 flags as **DECISION 4** (present tense on the homepage,
"rolling out" on `/demo` and `/reduce-project-delays`, `["en","hi"]` in schema).
The registry specifically noted this claim was what a buyer sees in search
results. It is now not. **If DECISION 4 resolves as "11 languages are live",
put it back** — there is room at 136 characters.

Verified after the rewrite: `npm run build` → 44 pages; `npm test` → 138/138 pass;
0 of 41 indexable pages over the title or description limit; still zero duplicate
titles or descriptions.

**Not yet live.** Per `CHANGE_LEDGER.md`, production still serves the pre-fix
code from `main`; this work sits on `fix/audit-2026-09-23` behind PR #8.

---

# Third pass — Google tools setup (2026-09-23, via Chrome)

Account driving all of this: **director@securedengineers.com** (Workspace, managed
by kalsi.co.in).

## The DNS TXT record for sotyn.ai

Add at GoDaddy → sotyn.ai → DNS → Add record:

| Field | Value |
|---|---|
| Type | `TXT` |
| Name / Host | `@` |
| Value | `google-site-verification=TVdC_GjFILrpoaFQ7ZsX540D9j1_Q2Iq9sRpCSGWD74` |
| TTL | default (1 hour) |

Then Search Console → the `sotyn.ai` domain property → **Verify**.

⚠️ The value above is the FULL record. Search Console's own input box visually
truncates it at `…Q2Iq9sRpCS` — the real token ends `…GWD74`. Read it from here,
not from a screenshot of that field.

⚠️ Re-check the token before you paste. The browser session was reset mid-setup,
so if the pending `sotyn.ai` property did not persist, Search Console will issue a
**new** token when you re-create it, and this one will not verify.

## Do NOT use the automated GoDaddy flow (recommended)

Search Console auto-detects GoDaddy and offers "START VERIFICATION", which runs
GoDaddy **Domain Connect**. That template's scope is
`domain-verification+gmail-setup` — it can write **MX records**, not only the
verification TXT. On a domain whose email you already run, that is an unnecessary
risk. The manual TXT record above changes nothing except verification.

A Domain Connect tab may still be open in Chrome from this session. It is inert
(GoDaddy was never signed into) — just close it.

## State found

| Tool | State before | Action |
|---|---|---|
| Search Console | **Empty.** One pending, unverified `securedengineers.com` domain property. Nothing for sotyn.ai | Created `sotyn.ai` domain property, pending DNS |
| Google Analytics | One GA4 property, `www.securedengineers.com` (live, receiving traffic). **Nothing for sotyn.ai** | Blocked — see below |
| Google Ads | An Ads account exists (the GA account is named "Google Ads Account"), so Keyword Planner needs no new account | Not reached |
| Google Tag Manager | Not checked | Blocked |
| Business Profile | Not checked | Blocked |

## Blocked, and why

Google demanded re-authentication ("Verify it's you") partway through, and GoDaddy
was never signed in. **Entering passwords is not something I do**, so these stop
with you:

1. Re-authenticate the Google session in Chrome.
2. Add the TXT record at GoDaddy (or tell me to drive the form once you're signed in).

Still outstanding after that: GA4 property + web stream for sotyn.ai, GTM
container, sitemap submission, Keyword Planner, Business Profile.

## Code prepared while blocked

`SITE.analytics` now has two **mutually exclusive** slots, because
`lead-and-analytics-spec.md` §2 says *"One stack only. Do not add a second tag
manager or a duplicate GA4 tag."*

- `ga4MeasurementId` — `G-XXXXXXXXXX`, loads gtag.js directly. Simplest.
- `gtmContainerId` — `GTM-XXXXXXX`, loads GTM; configure GA4 **inside** GTM.

Setting both throws at build time, so the spec cannot be violated by accident.
Both empty (the state today) means no analytics loads at all. `anonymize_ip` is
on and no PII is passed, per spec §2. `tests/analytics.test.mjs` asserts the
mutual exclusion, both ID formats, that nothing fires unconfigured, and that the
GSC slot holds a bare token rather than a whole meta tag.

Verified: `npm run build` → 47 pages; `npm test` → 163/163 pass.

**Still unresolved and not mine to decide:** the spec notes every form should link
to a privacy policy and *"that page does not exist yet"*. Loading GA4 or GTM
before it does widens that gap.
