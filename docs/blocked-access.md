# Blocked access — exactly what is needed, and from whom

Retrieved 2026-09-23. Nothing below was worked around with an estimate. Where a metric is missing, the audit says "no data", not a number.

| # | Tool / account | What happened | Exact permission or export needed | Who grants it | What it unblocks |
|---|---|---|---|---|---|
| 1 | **Google Search Console — sotyn.ai property** | The connected route exposes **Secured Engineers** properties, not sotyn.ai. Using them would mix an EPC services property with the software site. | Verify (or share) the **`https://www.sotyn.ai` property** — after the canonical-host change, add both `www.sotyn.ai` and the Domain property `sotyn.ai`. Grant **Full** user access to the working account, or export: Performance → last 16 months, dimensions query + page + date + country + device, CSV. | Whoever owns the sotyn.ai GSC property | Impressions/clicks baseline, branded vs non-brand split, near-ranking opportunities, indexing status, manual actions, sitemap read-back, URL Inspection |
| 2 | **GA4 property for sotyn.ai** | No GA4 or GTM tag found in the live page source — so there may be **no property at all**, or it is installed elsewhere. | Confirm whether a GA4 property exists. If yes: **Viewer** access + the measurement ID. If no: decide whether to create one before adding any tag. | Site owner | Landing-page sessions, acquisition, accepted-lead events, city data (with its accuracy limits) |
| 3 | **Google Tag Manager** | No container observed on the site. | Container ID + **Publish** or **Approve** rights, or confirmation that tags go directly in the Astro layout instead. | Site owner | One measurement stack, consent handling, DebugView validation |
| 4 | **Vercel project** | No dashboard access. Deployment provider is confirmed from response headers, but the **deployed commit cannot be proven** to match `main`. | Add the working account to the `sotyn-ai` Vercel project (Viewer is enough to read deployments; Developer to configure). Also confirm which of the five projects serves the domain. | Vercel account owner (ankur817) | Proof of deployed commit, env vars, preview URLs, build logs, domain config |
| 5 | **Lead webhook contract** (`securederp.in/api/public/sotyn-lead`) | `OPTIONS` and `GET` probed successfully. **No POST was sent** — that would create a real lead in production. | Either (a) the endpoint's success status, `Content-Type` and body shape, or (b) written authorisation to send one clearly-marked test lead, or (c) a staging endpoint. Also: does it return a lead id? Does it de-duplicate on `X-Request-Id`? | Whoever maintains securederp.in | Proving leads are durably stored; confirming the new strict client matches reality before it goes live |
| 6 | **CRM / ERP lead ledger** | No access. | Read access, or a dated export of leads by source for the last 6–12 months. | Site owner | Reconciling analytics against the sales ledger; measuring attended demos, paid pilots, won subscriptions, collected revenue |
| 7 | **Ahrefs** | Returned `Insufficient plan`. | A plan/seat covering Site Explorer + Keywords Explorer for the account in use. | Subscription owner | Backlinks, referring domains, competitor keyword overlap — all as **estimates**, labelled |
| 8 | **SEOcrawl** | Returned `billing_context_missing`. | Billing configured, or drop it — the crawl in this audit was done with plain HTTP requests and is sufficient. | Subscription owner | Nothing critical; optional |
| 9 | **Google Keyword Planner** | No authorised Google Ads account reached. | **Read** access to an existing Google Ads account (India, INR). Do **not** start a campaign to unlock ranges. | Ads account owner | Monthly search history and average-range data, clearly labelled as advertiser data — never as organic difficulty |
| 10 | **Google Trends exports** | No usable export obtained. | Nothing to grant — needs a session that can export CSV for India, 12-month and 5-year, with the exact keyword/topic definitions recorded. | — | Demand direction only. Normalised interest is not volume, and sparse data is not proof of zero demand |
| 11 | **CrUX / field performance** | Field data availability for this origin is reported in the performance appendix; where absent it is recorded as absent. | Nothing to grant; low traffic simply means no field data yet. A CrUX API key would allow a history pull. | — | p75 LCP / INP / CLS by mobile and desktop |
| 12 | **Google Business Profile** | Not assessed for eligibility. | Confirmation of **real staffed locations only**. The registered address is Ludhiana, Punjab. | Site owner | A single legitimate listing if eligible. No virtual offices, no per-city profiles, no keyword-stuffed names |

## Rule applied throughout
An inaccessible tool is recorded as inaccessible. It is never reported as "no traffic", "no demand" or "no issue", and no metric in the audit was filled in from memory, inference or a plausible-looking number.
